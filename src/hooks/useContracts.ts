// Re-export from shared WalletContext so all components share wallet state
export { useContracts, WalletProvider } from '../contexts/walletContext';
export type { ContractInstances } from '../contexts/walletContext';
